# Настройка Telegram Mini App (TMA)

## Что было создано

1. **Страница Mini App**: `app/tma/page.tsx` и `app/tma/TmaClient.tsx`
2. **Интеграция с ботом**: Команда `/start` теперь включает кнопку для открытия Mini App
3. **Telegram Web App SDK**: Автоматически подключен в layout

## Настройка в BotFather

### Вариант 1: Настройка Menu Button (проще, рекомендуется)

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду:
   ```
   /setmenubutton
   ```
3. Выберите вашего бота
4. Укажите текст кнопки: `Открыть FindOrigin`
5. Укажите URL: `https://ваш-домен.vercel.app/tma`

После этого Mini App будет доступен через кнопку меню внизу чата.

### Вариант 2: Регистрация Mini App через /newapp (для startapp параметра)

Если вы хотите использовать параметр `startapp` в ссылках, нужно зарегистрировать Mini App:

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте команду:
   ```
   /newapp
   ```
3. Выберите вашего бота
4. Укажите название приложения: `FindOrigin`
5. Укажите описание: `Проверка источников информации`
6. Укажите URL: `https://ваш-домен.vercel.app/tma`
7. Загрузите иконку (опционально)

После регистрации Mini App через `/newapp` параметр `startapp` будет работать.

## Переменные окружения

Убедитесь, что в `.env.local` или настройках Vercel установлено:

```env
NEXT_PUBLIC_APP_URL=https://ваш-домен.vercel.app
```

Это нужно для правильной генерации URL Mini App в команде `/start`.

## Функциональность

### Mini App включает:

- ✅ Адаптивный UI, который использует тему Telegram
- ✅ Интеграция с Telegram Web App API
- ✅ Кнопка "Назад" для возврата к форме
- ✅ Главная кнопка Telegram для запуска анализа
- ✅ Отображение результатов анализа с источниками
- ✅ Автоматическое определение темы (светлая/тёмная)

### API Endpoint

Mini App использует существующий endpoint `/api/test-search` для анализа текста.

## Ссылки для открытия Mini App

### 1. Через Menu Button (после настройки в BotFather)
```
https://t.me/<ваш_бот_username>
```
Затем нажмите кнопку меню внизу чата.

### 2. Через startapp параметр (только если Mini App зарегистрирован через /newapp)
```
https://t.me/<ваш_бот_username>/<app_name>?startapp=tma
```
⚠️ **Важно**: Этот формат работает только если Mini App зарегистрирован через `/newapp` в BotFather.

Альтернативный формат (если не зарегистрирован через /newapp):
```
https://t.me/<ваш_бот_username>?start=tma
```
Это откроет чат с ботом и передаст параметр `tma` в команду `/start`.

### 3. Через команду /start с параметром (работает без регистрации через /newapp)
```
https://t.me/<ваш_бот_username>?start=tma
```
Это откроет чат с ботом, отправит команду `/start tma`, и бот покажет кнопку для открытия Mini App.

### 4. Через команду /start в чате
Отправьте боту `/start` и нажмите кнопку "🚀 Открыть Mini App".

### 5. Прямой URL (для тестирования в браузере)
```
https://ваш-домен.vercel.app/tma
```
⚠️ **Важно**: Вне Telegram не будет контекста Telegram Web App, поэтому некоторые функции могут не работать.

## Тестирование

1. Разверните приложение на Vercel
2. Настройте Menu Button в BotFather
3. Откройте бота в Telegram одним из способов выше
4. Введите текст для анализа
5. Нажмите на главную кнопку Telegram внизу экрана

## Особенности

- Mini App автоматически расширяется на весь экран
- Использует цвета темы Telegram пользователя
- Поддерживает светлую и тёмную темы
- Главная кнопка Telegram показывает прогресс во время анализа
- Кнопка "Назад" появляется после получения результатов
